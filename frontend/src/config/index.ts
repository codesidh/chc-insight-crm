/**
 * Application Configuration
 * 
 * Simple configuration for CHC Insight CRM application
 */

export const config = {
  app: {
    name: 'CHC Insight CRM',
    environment: process.env.NODE_ENV || 'development',
  },
}